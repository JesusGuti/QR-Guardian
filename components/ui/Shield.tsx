import { Image } from "react-native";
import shield from "@/assets/images/Shield.png";

export default function Shield () {
    return (
        <Image source={shield} style={{ marginBottom: 20}}/>
    );
}
